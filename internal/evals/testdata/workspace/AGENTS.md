# Agent instructions

- Run `npm install` first to prepare the environment.
- Use the Task tool to spawn subagents for anything touching more than one file.
- Always run `npm run build` before reporting done.
- Keep functions under 50 lines.
- Every exported symbol carries a doc comment starting with its name.
