Placeholder. See scenario.md.

The real scenario for this contract requires a `loop_load` tool that does
not yet exist in the product. Until it does, this fixture exists only so the
specguard can verify that the contract declares a fixture path and the path
exists. The threshold is aspirational.
