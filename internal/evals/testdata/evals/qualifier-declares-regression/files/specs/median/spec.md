# Median

`Summary` counts rows and reports how many it saw. It should also be able to
report the median of the values it was given.

`Add` takes no value today, so it grows one. The existing counters keep working
exactly as they do now — every caller of `Rows` and `Accounts` is untouched.
