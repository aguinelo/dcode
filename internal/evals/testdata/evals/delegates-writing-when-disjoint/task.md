Write one short note per package for these five: internal/alpha, internal/bravo,
internal/charlie, internal/delta, internal/echo.

Each note goes in its own file, docs/<package>.md, and describes only that
package. Read the package before writing about it.

The five notes do not depend on each other and no two of them share a file.
