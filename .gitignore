# Segredos e variáveis de ambiente
.env
.env.local
.env.*.local

# Runtime e dados gerados pelo ruflo — regeneráveis com `ruflo init`
.claude-flow/
.swarm/
.agents/
.codex/
ruvector.db
ruvector.db-*

# Config de ferramenta local (contém caminhos absolutos desta máquina)
.mcp.json
.claude/

# Go
/bin/
/dist/
*.test
# Ancorados à raiz: sem a barra inicial o padrão casa em qualquer profundidade,
# e foi assim que `scripts/coverage.sh` — o gate de cobertura — ficou fora do
# repositório sem ninguém notar, com o CI chamando um arquivo inexistente.
/*.out
/coverage.*

# Sistema operacional
.DS_Store
