# Research: Loop do Agente

> Fonte da verdade de negócio para o ciclo que transforma uma instrução do usuário em trabalho executado.
> Depende de: **ADR-02** (sandbox), **ADR-03** (contexto append-only), **ADR-04** (client-server), **ADR-05** (provider).

## 1. Contexto

O loop é o coração do dcode. Ele orquestra motor de contexto, adaptador de provider, ferramentas e sandbox — todos os outros componentes existem para servi-lo.

É também onde o produto ganha ou perde caráter. Um loop que aborta ao primeiro erro de ferramenta produz um agente frágil. Um loop sem teto produz um agente caro. Um loop que executa ferramenta fora da fronteira produz um agente perigoso.

## 2. Fronteira de determinismo

**Regime: misto.** Esta é a spec onde a linha realmente importa.

| Parte | Regime | Verificação |
|---|---|---|
| Máquina de estados do turno | determinístico | asserção |
| Teto de iterações e detector de repetição | determinístico | asserção |
| Ordem de anexação de resultados | determinístico | golden file |
| Semântica de interrupção | determinístico | asserção |
| Gatilho e aplicação de compactação | determinístico | asserção |
| **Recuperação após erro de ferramenta** | **mediado por modelo** | limiar |
| **Encerramento do turno na hora certa** | **mediado por modelo** | limiar |

A mecânica é toda determinística. O que é mediado é **o que o modelo decide fazer** dentro dela.

## 3. User stories

| # | Como | Quero | Para |
|---|---|---|---|
| US-1 | usuário | descrever uma tarefa e ver o agente executá-la de ponta a ponta | é a razão do produto existir |
| US-2 | usuário | que uma ferramenta falhando não jogue fora o turno | falha de comando é rotina, não exceção |
| US-3 | usuário | interromper a qualquer momento sem corromper estado | agente que não se interrompe não é confiável |
| US-4 | usuário | que o agente não entre em loop infinito às minhas custas | custo previsível |
| US-5 | usuário | que uma tarefa longa não morra ao encher a janela | tarefa real não cabe numa janela |
| US-6 | usuário | ver a resposta e as ações enquanto acontecem | acompanhar e intervir cedo |

## 4. Regras de negócio

### RN-1 — O turno é um ciclo até não haver mais ferramenta a chamar
```
1. anexa entrada do usuário ao histórico
2. monta contexto            ← ponto de checagem de compactação
3. streaming do provider
4. sem tool call? → critério de pronto pendente? (RN-10) → não: turno completo · FIM
5. executa as tool calls, anexa resultados
6. incrementa iteração; estourou teto? → turno termina em erro
7. volta ao 2
```

### RN-9 — Turno com mudança não verificada não está completo
O passo 4 deixa de ser "sem tool call, acabou". Havendo arquivo editado e verificação `stale` ou `failed`, o lembrete é anexado e o ciclo continua — **uma vez por turno**.

Custa uma iteração quando dispara, e compra a diferença entre relato falso e relato honesto. A regra de negócio é a RN-13 de `202608080016-behavior-definition`; aqui fica só onde ela é aplicada no ciclo.

Persistindo sem verificação, o turno encerra em `StopUnverified` — **estado, não erro**. Sem esse teto, projeto cuja verificação não roda gira até o teto de iterações da RN-2, o que é a defesa errada para o problema errado.

Verificação `unavailable` — mudou e não há comando conhecido — **não** força continuação: não há o que rodar, e insistir só produz outro palpite.

> **Esta é a instância unitária da RN-10.** A verificação é uma lista de critérios com um item, e os dois mecanismos não coexistem — a reentrada, a medida de progresso e o encerramento honesto são os da RN-10. O que esta regra preserva é *qual* critério vale por default quando ninguém declarou nenhum: ter rodado a verificação do projeto.

### RN-10 — Pronto é condição conferida, e a saída é por progresso
O passo 4 deixa de ser "sem tool call, acabou". Havendo critério de pronto não cumprido, o ciclo reentra — **enquanto houver progresso**.

**Critério é fato, nunca prosa.** Comando mais código de saída esperado. Critério julgado pelo modelo devolve ao modelo a decisão de pronto, agora tendo gasto turnos no caminho.

**Progresso é o conjunto de não cumpridos encolher estritamente** entre ciclos. Não é "o modelo acha que avançou" nem "rodou alguma ferramenta". Conjunto que cresce é regressão, e conta como não-progresso.

Não é "até estar 100%", e a razão é o modo de falha que essa formulação cria: **um agente que não pode sair passa a satisfazer o medidor em vez do objetivo.** Se a saída é teste verde e ele não consegue, o caminho mais curto é enfraquecer o teste. O laço existiria para impedir relato falso e produziria teste falso — que é pior, porque relato falso se descobre rodando e teste falso fica no repositório fingindo cobertura.

Parado o progresso, o turno encerra com **a lista do que ficou por cumprir**. Isso é resultado, não erro: é o produto sendo honesto sobre trabalho que precisa de uma pessoa. Tratar como erro cria o incentivo de desligar a checagem.

Critério que não tem como rodar **não** provoca reentrada — não há o que executar, e insistir produz outro palpite. Ele entra no relatório como não conferido.

**E o agente não pode mexer no que o mede**, sem o que o resto é teatro: o arquivo da definição está na classe de `.dcode/**`, que já exige confirmação de escrita, e mudança em caminho declarado como medição é **destacada, nunca contada como progresso em silêncio**. Não é proibição — corrigir o teste às vezes é o trabalho certo. É visibilidade.

### RN-11 — Delegação é somente-leitura, e o relatório diz onde olhou
Uma cabeça só significa que tarefa atravessando vinte arquivos lê os vinte na mesma janela, e cada um empurra o anterior para fora. Delegar inverte: o filho lê na janela **dele** e devolve meia página. O ganho não é velocidade — é que **o custo da leitura não volta**.

**Somente leitura, por construção.** Os quatro problemas conhecidos de delegação — conflito de escrita, herança de aprovação, desfazimento, conferência — existem porque o filho escreve. Três somem quando ele só lê. O turno delegado nasce em `ModeReadOnly`, que a ferramenta fixa e não expõe; não é parâmetro que o modelo passa nem campo que se esquece de preencher.

**Aprovação não se herda** (ADR-02): consentimento dado ao pai não vale para o filho. Logo turno delegado **nunca pede aprovação** — leitura barrada por regra é negada e **reportada**. Não é prompt porque o usuário fez uma pergunta e ser interrompido por N filhos destrói o ganho de ter delegado; não é silêncio porque conclusão com buraco não declarado é conclusão errada com cara de completa.

**O relatório traz os caminhos lidos**, não só a conclusão. *"Não achei nada errado"* — não achou, ou não olhou? Refazer para conferir anula o ganho; a lista custa quase nada e transforma "confie em mim" em algo conferível por amostragem.

**O filho recebe a tarefa, não o histórico do pai** — histórico copiado devolveria o custo que a delegação existe para evitar.

Quatro limites, nenhum opcional: teto de iterações próprio; tokens do filho debitados do orçamento **do pai**, senão o teto do pai é ficção; **sem aninhamento**, garantido por o registro do filho não conter a ferramenta; e resultado com teto e truncamento declarado.

### RN-2 — Dois limites, com papéis diferentes
**Teto de iterações** é backstop, dimensionado pelo caso legítimo: um refactor cruzando dez arquivos consome de trinta a cinquenta chamadas de ferramenta. Teto que quebra trabalho real vira teto que o usuário desliga.

**Detector de repetição** é o mecanismo real: N chamadas idênticas consecutivas — mesma ferramenta, mesmo input — indicam loop patológico e travam muito antes do teto.

A identidade é sobre o **input**, não sobre a ferramenta. Isso é o que faz o detector conviver com RN-4: tentar de novo com abordagem diferente não é chamada idêntica.

O teto de tokens existe como controle de custo, mas **não é a defesa primária** — cortar por token no meio do turno deixa o workspace meio-editado, estado pior que o inicial. O teto de iterações já limita o gasto por construção.

### RN-3 — Ferramentas executam em paralelo; a ordem não é sequencial perfeita
Tool calls independentes de um mesmo passo executam concorrentemente.

Três consequências obrigatórias:

1. **Anexação em ordem determinística**, pelo índice em que o modelo as emitiu — nunca pela ordem de término. É o que mantém o histórico reproduzível e golden-testável.
2. **O modelo é informado** de que a execução foi concorrente e que os resultados não refletem ordem temporal. Ele não pode assumir que a ferramenta 1 terminou antes de a 2 começar.
3. **O log registra a ordem real** — início e fim de cada execução — para que a depuração enxergue o que a anexação esconde.

**Conflito serializa.** Chamadas que tocam o mesmo caminho de arquivo, ou que executam comando de sistema, não correm em paralelo entre si. Paralelismo vale para leitura e busca, onde é seguro e onde está o ganho real.

### RN-4 — Erro de ferramenta alimenta o modelo, nunca aborta o turno
O resultado do erro é anexado como resultado de ferramenta e o loop continua. O modelo reanalisa e decide: seguir mesmo assim, repetir, tentar caminho alternativo — inclusive por comando de sistema — ou desistir e relatar.

Para essa decisão ser possível, o erro precisa ser **informativo**: o que foi tentado, o que falhou, e o sinal disponível. Uma string genérica força o modelo a adivinhar, e adivinhar é como um agente corrompe arquivo.

Abortar o turno ao primeiro erro é o erro clássico de design de harness. O modelo quase sempre se recupera sozinho, e recuperar é justamente o que se espera de um agente.

### RN-5 — Interrupção é possível em todo ponto de espera
O usuário interrompe entre passos, durante o streaming e durante a execução de ferramenta. O turno encerra com estado consistente: nenhuma escrita pela metade, nenhum resultado órfão no histórico.

Ferramenta já iniciada quando a interrupção chega **termina** ou é cancelada, mas seu resultado é anexado de qualquer forma se ela produziu efeito. Histórico que mente sobre o que aconteceu no disco é pior que histórico incompleto.

### RN-6 — Compactação é verificada uma vez por iteração, no passo 2
Só ali. Verificar em outro ponto convida a compactação incremental, que a ADR-03 proíbe.

Gerar o texto do resumo é chamada ao modelo e pertence ao loop, não ao motor de contexto — é o que mantém aquele componente puro.

### RN-7 — Toda ferramenta passa pela política antes de executar
Sem exceção e sem caminho alternativo. Execução direta que contorne o avaliador de política é blocker imediato.

Cruzar a fronteira bloqueia o turno e pede aprovação; sem resposta no prazo, **nega** — falha fechada, conforme a spec do protocolo.

### RN-8 — Todo fato observável vira evento antes de qualquer outra coisa
O loop não escreve em terminal nem em arquivo de log como caminho primário: ele emite evento. Quem exibe é o cliente.

É o que sustenta a ADR-04 — a mesma sessão observada por TUI, IDE ou nenhum cliente se comporta de forma idêntica.

## 5. Fora de escopo

- Implementação das ferramentas — spec própria.
- Fronteira do sandbox e sua aplicação pelo sistema operacional — spec própria.
- **Escrita** delegada e execução paralela de múltiplos filhos. A leitura delegada é a RN-11; escrita exige worktree isolado, reconciliação e desfazimento, e é a única parte que pode corromper o repositório.
- Retomada de turno após queda do servidor.

## 6. Changelog

- [202608102000 — Verificação antes da afirmação](../behavior-definition/changelog/202608102000-verificacao-antes-da-afirmacao.md)
- [202608102100 — Definição de pronto](changelog/202608102100-definicao-de-pronto.md)
- [202608102500 — Delegação somente-leitura](changelog/202608102500-delegacao-somente-leitura.md)
